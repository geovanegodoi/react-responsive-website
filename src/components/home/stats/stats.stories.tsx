import type { Meta } from '@storybook/react';
import { Stats } from './stats';

const Story: Meta<typeof Stats> = {
  component: Stats,
  title: 'Components/Home Page/Stats',
};
export default Story;

export const Primary = {
  args: {},
};
