import type { Meta } from '@storybook/react';
import { Cloud } from './cloud';

const Story: Meta<typeof Cloud> = {
  component: Cloud,
  title: 'Components/Home Page/Cloud',
};
export default Story;

export const Primary = {
  args: {},
};
