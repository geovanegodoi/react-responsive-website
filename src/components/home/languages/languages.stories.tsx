import type { Meta } from '@storybook/react';
import { Languages } from './languages';

const Story: Meta<typeof Languages> = {
  component: Languages,
  title: 'Components/Home Page/Languages',
};
export default Story;

export const Primary = {
  args: {},
};
