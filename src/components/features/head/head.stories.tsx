import type { Meta } from '@storybook/react';
import { Head } from './head';

const Story: Meta<typeof Head> = {
  component: Head,
  title: 'Components/Features Page/Head',
};
export default Story;

export const Primary = {
  args: {},
};
