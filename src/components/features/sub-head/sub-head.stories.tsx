import type { Meta } from '@storybook/react';
import { SubHead } from './sub-head';

const Story: Meta<typeof SubHead> = {
  component: SubHead,
  title: 'Components/Features Page/SubHead',
};
export default Story;

export const Primary = {
  args: {},
};
