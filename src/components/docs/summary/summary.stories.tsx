import type { Meta } from '@storybook/react';
import { Summary } from './summary';

const Story: Meta<typeof Summary> = {
  component: Summary,
  title: 'Components/Docs Page/Summary',
};
export default Story;

export const Primary = {
  args: {},
};
